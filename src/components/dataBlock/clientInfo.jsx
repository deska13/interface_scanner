import React from 'react'

const clientInfo = (client) => {
    return (
        <div>
            {/* <h3>
                Клиент: {client.surname} {client.name} {client.patronymic}
            </h3>
            <ResetInputTextData
                textHeader="Фамилия"
                onChange={e => setPassport({...client, surname: e.target.value})}
                placeholder={client.surname}
            />
            <ResetInputTextData
                textHeader="Имя"
                onChange={e => setPassport({...client, name: e.target.value})}
                placeholder={client.name}
            />
            <ResetInputTextData
                textHeader="Отчество"
                onChange={e => setPassport({...client, patronymic: e.target.value})}
                placeholder={client.patronymic}
            />
            <ResetInputTextData
                textHeader="День рождения"
                onChange={e => setPassport({...client, birthday: e.target.value})}
                placeholder={client.birthday}
            />
            <ResetInputTextData
                textHeader="Пол"
                onChange={e => setPassport({...client, birthday: e.target.value})}
                placeholder={client.sex}
            />
            <ResetInputTextData
                textHeader="ИНН"
                onChange={e => setPassport({...client, birthday: e.target.value})}
                placeholder={client.sex}
            /> */}
        </div>
    )
}

export default clientInfo
